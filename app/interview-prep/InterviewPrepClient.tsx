"use client";

import { useMemo, useState } from "react";

const questions = [
  { category: "Fundamentals", level: "Beginner", question: "What is API testing?", answer: "API testing verifies requests, responses, business rules, security, data integrity, and performance at the service layer without depending on the user interface.", points: ["Mention request and response validation", "Explain why API tests are faster than UI tests", "Give a practical example"] },
  { category: "Fundamentals", level: "Beginner", question: "What is the difference between GET, POST, PUT, PATCH, and DELETE?", answer: "GET reads, POST creates or triggers, PUT replaces a complete resource, PATCH changes part of a resource, and DELETE removes it. Also discuss safety and idempotency rather than memorising only CRUD.", points: ["PUT and DELETE are normally idempotent", "POST is generally not idempotent", "PATCH semantics depend on the API"] },
  { category: "HTTP", level: "Beginner", question: "How do you validate an API response?", answer: "Validate the status, headers, schema, important field values, business rules, response time, and side effects in downstream data.", points: ["Avoid checking only 200", "Validate negative responses", "Confirm database or follow-up API state when appropriate"] },
  { category: "HTTP", level: "Intermediate", question: "Explain 400, 401, 403, 404, 409, 415, 422, 429, and 500.", answer: "400 means malformed or invalid input; 401 missing or invalid authentication; 403 authenticated but forbidden; 404 missing resource; 409 state conflict; 415 unsupported body format; 422 valid syntax but failed business validation; 429 rate limited; and 500 unexpected server failure.", points: ["Use a short example for each", "Distinguish 401 from 403", "Mention Retry-After for 429"] },
  { category: "Authentication", level: "Intermediate", question: "How do you test Bearer-token authentication?", answer: "Test a valid token, missing token, malformed scheme, expired token, tampered token, insufficient scope, token reuse, and protected-data isolation.", points: ["Never log real secrets", "Verify 401 versus 403", "Check expiry and refresh behaviour"] },
  { category: "Automation", level: "Intermediate", question: "How would you design a Rest Assured API framework?", answer: "Separate configuration, request specifications, endpoint clients, payload models or builders, test data, assertions, utilities, reporting, and environment management. Keep tests readable and avoid duplicating authentication or headers.", points: ["Use RequestSpecification and ResponseSpecification", "Support parallel execution safely", "Keep credentials outside source code"] },
  { category: "Automation", level: "Intermediate", question: "What should an API automation test assert?", answer: "Assert the expected status and contract first, then important values, headers, response time, and business side effects. Assertions should be stable and tied to the requirement.", points: ["Do not assert dynamic values exactly", "Use JSONPath or schema validation", "Include useful failure messages"] },
  { category: "Automation", level: "Advanced", question: "How do you perform data-driven API testing?", answer: "Separate the request template from test rows, load controlled CSV, JSON, or generated data, execute each row independently, compare expected and actual results, and report row-level failures without hiding the dataset that caused them.", points: ["Include positive and negative rows", "Avoid shared mutable data", "Limit and clean up created records"] },
  { category: "Design", level: "Advanced", question: "What is idempotency and how would you test it?", answer: "An idempotent request produces the same intended state when repeated. Send the same request multiple times with the same idempotency key and verify that no duplicate transaction is created and that the API returns a consistent result.", points: ["Common for payment APIs", "Verify state, not only status", "Test a reused key with changed payload"] },
  { category: "Design", level: "Advanced", question: "How do you test pagination?", answer: "Validate default and custom page sizes, first and last pages, empty pages, total counts, stable ordering, invalid parameters, duplicates or missing records across pages, and performance with large datasets.", points: ["Test filtering and sorting together", "Check boundaries", "Consider cursor-based pagination"] },
  { category: "Scenarios", level: "Advanced", question: "A payment API intermittently returns 503. What would you test?", answer: "Capture correlation IDs, confirm the failure is safe, verify Retry-After, apply bounded retries only for idempotent operations, check backoff and monitoring, and verify that retries never create duplicate payments.", points: ["Do not retry every error", "Use exponential backoff with limits", "Verify final system state"] },
  { category: "Scenarios", level: "Advanced", question: "The API returns 200 but a required field is missing. Is the test passed?", answer: "No. The transport succeeded but the response contract failed. A robust test validates required fields or the JSON schema and reports a contract defect.", points: ["Status validation alone is insufficient", "Explain schema validation", "Assess consumer impact"] },
  { category: "Scenarios", level: "Intermediate", question: "How would you test a money-transfer API?", answer: "Cover valid transfers, insufficient funds, zero or negative amounts, same source and destination, unknown accounts, currency rules, limits, authorization, idempotency, concurrency, ledger entries, and atomic balance updates.", points: ["Debit and credit must be atomic", "Check duplicate submission", "Verify transaction history"] },
  { category: "Performance", level: "Advanced", question: "What API performance checks belong in functional automation?", answer: "Use a reasonable response-time threshold as a regression signal, but keep load and capacity testing separate. Track percentiles, warm-up behaviour, payload sizes, and dependency latency in dedicated performance tests.", points: ["Avoid flaky overly strict thresholds", "Do not confuse one request with load testing", "Monitor trends"] },
  { category: "Project", level: "Intermediate", question: "Explain your Banking API TestLab project.", answer: "Describe the problem first: a safe, public place to practise realistic banking APIs. Then cover token authentication, customer and multi-account flows, transactions, Swagger, Postman, assertions, negative tests, challenges, persistent isolated sessions, automation generation, and Cloudflare deployment.", points: ["Explain your own engineering decisions", "Demonstrate one end-to-end flow", "Mention what you would improve next"] },
  { category: "Project", level: "Advanced", question: "What technical challenges did you solve in the TestLab?", answer: "Discuss persistent session-isolated data, token expiry, realistic state-dependent banking rules, public serverless deployment, request chaining with saved IDs, reliable JSON handling, safe error simulation, and balancing beginner guidance with advanced practice.", points: ["Use specific examples", "Explain trade-offs", "Own the lessons learned"] },
];

export default function InterviewPrepClient() {
  const [query, setQuery] = useState("");
  const [category, setCategory] = useState("All");
  const [level, setLevel] = useState("All");
  const [mockIndex, setMockIndex] = useState(0);
  const [answerVisible, setAnswerVisible] = useState(false);
  const categories = ["All", ...Array.from(new Set(questions.map((item) => item.category)))];
  const filtered = useMemo(() => questions.filter((item) => (category === "All" || item.category === category) && (level === "All" || item.level === level) && [item.question, item.answer, item.category].join(" ").toLowerCase().includes(query.toLowerCase())), [query, category, level]);
  const mockQuestion = questions[mockIndex % questions.length];

  function nextMock() { setMockIndex((current) => (current + 1) % questions.length); setAnswerVisible(false); }

  return <main className="interview-page"><header className="tool-nav"><a href="/"><span>B</span><strong>BetappaBharath <em>API TestLab</em></strong></a><nav><a href="/sdet-lab">SDET Lab</a><a href="/demo">2-Min Demo</a><a href="/#playground">API Practice</a></nav></header><section className="interview-hero"><div><p>Interview preparation</p><h1>Explain API testing with confidence.</h1><span>Clear answers, practical scenarios, and project-based questions for QA Automation and SDET interviews.</span></div><aside><b>{questions.length}</b><span>curated questions</span><small>Beginner to advanced</small></aside></section><section className="mock-interview"><div><p>Mock interview mode</p><h2>{mockQuestion.question}</h2><span>{mockQuestion.category} · {mockQuestion.level}</span>{answerVisible && <div><p>{mockQuestion.answer}</p><ul>{mockQuestion.points.map((point) => <li key={point}>{point}</li>)}</ul></div>}</div><aside><button type="button" onClick={() => setAnswerVisible((current) => !current)}>{answerVisible ? "Hide answer" : "Reveal answer"}</button><button type="button" className="mock-next" onClick={nextMock}>Next question →</button></aside></section><section className="question-bank"><div className="question-heading"><div><p>Question bank</p><h2>Search and practise by topic</h2></div><div><input type="search" placeholder="Search questions..." value={query} onChange={(event) => setQuery(event.target.value)} /><select value={level} onChange={(event) => setLevel(event.target.value)}><option>All</option><option>Beginner</option><option>Intermediate</option><option>Advanced</option></select></div></div><div className="category-filter">{categories.map((item) => <button type="button" key={item} className={category === item ? "active" : ""} onClick={() => setCategory(item)}>{item}</button>)}</div><div className="question-list">{filtered.map((item, index) => <details key={item.question}><summary><span>{String(index + 1).padStart(2, "0")}</span><div><b>{item.question}</b><small>{item.category} · {item.level}</small></div></summary><div><p>{item.answer}</p><b>Strong answer checklist</b><ul>{item.points.map((point) => <li key={point}>{point}</li>)}</ul><a href={item.category === "Project" ? "/demo" : "/#playground"}>{item.category === "Project" ? "Review the project demo" : "Practise this concept"} →</a></div></details>)}{!filtered.length && <div className="no-questions">No matching questions. Try another keyword or filter.</div>}</div></section><section className="interview-tips"><h2>Use the STAR structure for scenario answers</h2><div><article><span>S</span><b>Situation</b><p>Give the project or defect context.</p></article><article><span>T</span><b>Task</b><p>Explain what you needed to verify or solve.</p></article><article><span>A</span><b>Action</b><p>Describe your testing and technical decisions.</p></article><article><span>R</span><b>Result</b><p>Share the measurable outcome and lesson.</p></article></div></section><footer>Speak naturally—understand the concept instead of memorising every sentence.</footer></main>;
}
