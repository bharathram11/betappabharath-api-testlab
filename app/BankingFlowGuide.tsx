const steps = [["1", "Create token", "Temporary access pass", "POST /auth/token"], ["2", "Create customer", "Receive Customer ID", "POST /customers"], ["3", "Read or update customer", "Use Customer ID", "GET / PUT /customers/{id}"], ["4", "Create accounts", "One customer can have many", "POST /customers/{id}/accounts"], ["5", "Move money", "Credit, debit, or transfer", "POST /transactions or /transfers"], ["6", "View or delete", "Check history; delete zero-balance account", "GET /transactions · DELETE /accounts/{id}"]];
export default function BankingFlowGuide() { return <section className="banking-flow-guide"><div><p className="eyebrow">Keep this flow in mind</p><h2>How a banking API works</h2><p className="flow-intro">Create a customer, give the customer one or more accounts, then perform banking actions on those accounts.</p></div><div className="flow-steps">{steps.map(([number, title, copy, endpoint]) => <article key={number}><span>{number}</span><div><h3>{title}</h3><p>{copy}</p><code>{endpoint}</code></div></article>)}</div><p className="flow-note"><b>Remember:</b> Create Customer returns a Customer ID. Create Account returns an Account ID. Use those IDs in the next requests.</p></section>; }
